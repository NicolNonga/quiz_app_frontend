import { IerrorForm } from "../interface/errorForm.interface";

export const ERRO_MESSAGES:IerrorForm = {
  required: "This field is required",
  minlength: "The entered value is too short",
  maxlength: "The entered value is too long",
};
